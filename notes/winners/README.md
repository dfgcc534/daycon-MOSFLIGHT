# notes/winners/

수상자 노트북 + 메타데이터.

각 노트북은 짝 `{handle}.ipynb` + `{handle}.meta.yaml` 로 둔다 (메타데이터 스키마 `WINNERS.md §2`).

대응 재현 plan = `plans/plan-w-{NNN}-{handle}.md` (`plans/plan-template-winner.md` 골격).

## 현재 보유

| handle | rank | source | meta | 재현 plan | status |
|---|---|---|---|---|---|
| (없음 — 대회 종료 후 download) | | | | | |

## 추가 절차

1. DACON 코드공유에서 download → `{handle}.ipynb` 배치
2. `{handle}.meta.yaml` 작성 (`WINNERS.md §2` schema 그대로)
3. lane `w` 의 미사용 번호로 `plans/plan-w-{NNN}-{handle}.md` 발행
4. 본 표 row 1개 추가
